export const initialState = {
    id:"",
    username:"",
    password:"",
}

export const signupReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
        return state;
    }
}