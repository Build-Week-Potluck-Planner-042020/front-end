// import


export const banna = {
    id:"",
    username:"",
    password:"",
}

export const authReducer = (state = banna, action) => {
    switch (action.type) {
        default:
        return state;
    }
}